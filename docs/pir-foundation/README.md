# PIR Foundation Package (provenance)

Source adjudication package for the Physical Intermediate Representation (PIR)
Stage-1 substrate. Landed here so ADJUDICATION / work-order / architecture
references remain auditable without overwriting the evolved live tree.

| File | Role |
|---|---|
| `FINAL_ANALYSIS.md` | Consolidated post-adjudication design basis |
| `ADJUDICATION.md` | Accept / accept-modified / reject / defer table |
| `AGENT_WORK_ORDER_v2.md` | Stage-1 coding agent work order |
| `architecture.yaml` | Target architecture v0.2 (adjudicated) |
| `engineering_backlog.csv` | Stage-gated backlog |
| `README_PACKAGE.md` | Original package index |

**Not copied from the package:** `schemas/`, `examples/`, and `manifest.json`.
Those drafts were adapted in-tree under `pir/` and `examples/pir/` (see
`docs/adr/ADR-PIR-0001.md` for SPEC deviations such as `AMBIGUOUS` and
`measurement_interface`).

Original zip: `archive/sprint-drops/gv-reverse-engineering-sprint.zip`
(nested `PIR_Foundation_Package_v2.zip`).
