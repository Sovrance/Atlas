# Atlas RH--Weil Mathematical Parity Engineering Specification v0.2

**Document ID:** ATLAS-RH-ENG-002\
**Date:** 2026-08-16\
**Target baseline:** Sovrance/Atlas after PR #5, merge `1fda9b4`\
**Purpose:** Bring the repository's reproducible RH/Weil implementation
to mathematical parity with RH Research Notebook V2 through Run 18,
without promoting imported notebook numbers and without making an RH
proof claim.

## 0. Executive decision

PR #5 successfully landed the RH/Weil scaffold, exact identities, E0
scalar cell structure, Fourier diagnostic scaffold, Connes-CvS adapter,
and certificate machinery. The repository is one certification stage
behind the notebook.

The next sprint SHALL close the gap in this order:

1.  regenerate the cutoff-free scalar numerical certificate as E1;
2.  regenerate the cutoff-free degree-1 centered-odd certificate as E1;
3.  regenerate the cutoff-free degree-2 compact/bubble certificate as
    E1;
4.  replace the current heuristic T=84 Fourier energy probe with the
    **true finite Weil Gram**;
5.  reproduce the Run-16 direct-Fourier point certificates;
6.  implement exact support-length Taylor jets and certify the Run-18
    shape statements uniformly in L;
7.  promote the direct T=84 degree-2 result to E1 only after interval
    coverage closes;
8.  emit E0/E1/E3 PIR facts with assumption/provenance taint;
9.  only then unblock degree 3.

No imported notebook value is evidence. Every E1 number must be
regenerated by code committed to Atlas.

------------------------------------------------------------------------

# 1. Current baseline and gap

Current gates:

  Gate                    Current           Required
  ----------------------- ----------------- -----------------------
  Atlas CI                PASS              remain PASS
  exact identities        E0 regenerated    retain
  scalar cell structure   E0 regenerated    retain + numerical E1
  degree 1                algebraic only    dedicated E1
  degree 2 compact        algebraic only    dedicated E1
  T=84 Fourier            E3 energy probe   true Weil Gram + E1
  PIR bridge              absent            E0/E1/E3 facts
  RH claim boundary       enforced          remain enforced

The current `math/rh_weil/src/fourier.py::fourier_even_gram_entries` is
explicitly a diagnostic `|H|^2` energy probe. It MUST NOT be renamed or
silently promoted. Either retain it as `fourier_energy_probe` or
deprecate it after the true Weil implementation lands.

------------------------------------------------------------------------

# 2. Locked mathematical conventions

## 2.1 Assembly

The canonical convention remains

\[ G = G\^0 - G\^p + G\^`\infty`{=tex}. \]

All new certificates MUST carry this normalization string and
`rh_proof_claim: false`.

Domain for this sprint:

\[ L`\in[\log 3,\log 4]`{=tex}. \]

The prime-power cell must be handled with exact breakpoint bookkeeping.
Do not evaluate an inactive prime shift.

## 2.2 Polynomial basis

Use midpoint parity.

Odd: \[ q_1(x)=x-`\frac `{=tex}L2. \]

Even bubble: \[ b(x)=x(L-x). \]

Centered quadratic: \[
q_2=x\^2-Lx+`\frac{L^2}{6}`{=tex}=`\frac{L^2}{6}`{=tex}-b. \]

Key determinant identities:

\[ D\_{2,T}=E\_{2,T}+L\^2G\_{00,T}O\_{1,T}, \]

\[ `\det `{=tex}G\_{`\deg`{=tex}`\le2`{=tex},T}=O\_{1,T}E\_{2,T}. \]

where

\[ O\_{1,T}=G_T\[q_1,q_1\], `\qquad`{=tex}
E\_{2,T}=`\det `{=tex}G\_{T,{1,b}}. \]

------------------------------------------------------------------------

# 3. WO-RH-09 --- Certified scalar numerical verifier

## Goal

Promote the scalar result from structural E0 to regenerated E1.

The scalar verifier must evaluate the complete cutoff-free scalar entry
on each prime-power cell, use strict convexity, isolate at most one
stationary point per cell, and certify the minimum.

Inherited notebook target, **for regression comparison only**:

\[ G\_{00}(L)\>0.0753795566117244. \]

This number is NOT the acceptance criterion. Acceptance is a newly
regenerated strictly positive interval lower bound with auditable
provenance. The regenerated value should agree with the notebook within
its certified enclosure.

## Required implementation

Add:

``` text
math/rh_weil/src/archimedean.py
math/rh_weil/src/interval_backend.py
math/rh_weil/src/weil_scalar.py
math/rh_weil/scripts/certify_scalar_e1.py
math/rh_weil/tests/test_scalar_e1.py
math/rh_weil/certificates/e1_scalar_log3_log4.json
```

Preferred rigorous backend: `python-flint` Arb/Acb. If unavailable, the
E1 job MUST fail/skip explicitly; it may not fall back to mpmath and
still emit E1.

The scalar cell logic already proves

\[ W\_{00}''(L) =
`\frac{2(r^3-r-1)}{\sqrt r(r^2-1)}`{=tex},`\quad `{=tex}r=e\^L, \]

and the derivative jump at (q=p\^k) is

\[ W\_{00}'((`\log `{=tex}q)^+)-W\_{00}'((`\log `{=tex}q)^-) =
-`\frac{2\log p}{\sqrt q}`{=tex}. \]

Use these facts to reduce interval work.

## Acceptance

-   rigorous positive lower bound on all of `[log(3),log(4)]`;
-   certificate `evidence_class=E1`, analyzer `SOUND`;
-   interval precision, subdivisions, source hashes, backend/version
    recorded;
-   independent regression against the imported notebook value, but no
    dependency on it.

------------------------------------------------------------------------

# 4. WO-RH-10 --- Dedicated degree-1 E1 certificate

## Goal

Regenerate the centered odd pivot:

\[ O_1(L)=G\[q_1,q_1\]\>0. \]

Notebook regression target:

\[ O_1\>0.0142397928900392393805. \]

Again, regenerate rather than import.

## First-principles formulas

Prime overlap:

\[ K\_{q_1q_1}(a;L) = `\frac{(L-a)(L^2-2La-2a^2)}`{=tex}6. \]

Every active shift on the current cell has (a/L`\ge1`{=tex}/2), hence
this kernel is negative. The sign proof should remain E0 and be
referenced by the E1 certificate.

Pole term:

\[ A(L)=L`\cosh`{=tex}(L/4)-4`\sinh`{=tex}(L/4), \]

\[ G^0\[q_1,q_1\]=-8A(L)^2. \]

The compact archimedean term must be evaluated rigorously using the
canonical normalization.

## Required files

``` text
math/rh_weil/src/weil_degree1.py
math/rh_weil/scripts/certify_degree1_e1.py
math/rh_weil/tests/test_degree1_e1.py
math/rh_weil/certificates/e1_degree1_log3_log4.json
```

## Acceptance

-   `G00>0` E1 dependency referenced by certificate ID;
-   `O1>0` uniformly;
-   degree-1 matrix PD inferred via midpoint parity;
-   no floating scan used as warrant.

------------------------------------------------------------------------

# 5. WO-RH-11 --- Compact real-space degree-2 E1 certificate

## Goal

Regenerate the compact/bubble degree-2 positivity certificate before
attempting the independent Fourier proof.

Use the even basis ({1,b}).

Prime kernels:

\[ K\_{00}=2(L-a), \]

\[ K\_{0b}=`\frac{(L-a)^2(L+2a)}`{=tex}3, \]

\[ K\_{bb} = `\frac{(L-a)^3(L^2+3La+a^2)}{15}`{=tex}. \]

Pole helper:

\[ E_b\^+ = 4\[(L-4)e\^{L/2}+L+4\], \]

\[ E_b\^- = 4\[(L-4)+(L+4)e\^{-L/2}\]. \]

The even pole block has rank at most one and determinant zero. This must
be tested exactly/numerically as a regression invariant.

Define

\[ E_2(L)=G\_{00}G\_{bb}-G\_{0b}\^2. \]

Notebook regression target:

\[ E_2\>4.07220283229438138`\times10`{=tex}\^{-6}. \]

## Required files

``` text
math/rh_weil/src/weil_degree2.py
math/rh_weil/scripts/certify_degree2_compact_e1.py
math/rh_weil/tests/test_degree2_compact_e1.py
math/rh_weil/certificates/e1_degree2_compact_log3_log4.json
```

## Acceptance

-   rigorous `E2>0` over the full cell;
-   raw even determinant computed from \[ D_2=E_2+L\^2G\_{00}O_1; \]
-   full degree-2 determinant computed from \[
    `\det `{=tex}G\_{`\deg`{=tex}`\le2`{=tex}}=O_1E_2; \]
-   E1 certificate references scalar and degree-1 certificate IDs.

------------------------------------------------------------------------

# 6. WO-RH-12 --- Replace the T=84 probe with the true finite Weil Gram

## Critical requirement

Do not certify the current `E2_probe`.

Implement the actual finite-cutoff Weil matrix:

\[ G\_{ij,T} = G^0\_{ij}-G^p\_{ij} +
`\frac1`{=tex}`\pi`{=tex}`\int`{=tex}*0\^T h*+(t),
`\operatorname{Re}`{=tex}(`\overline{H_i(t;L)}`{=tex}H_j(t;L)),dt, \]

with

\[ h\_+(t)=
`\operatorname{Re}`{=tex}`\psi`{=tex}`\left`{=tex}(`\frac14`{=tex}+`\frac{it}{2}`{=tex}`\right`{=tex})-`\log`{=tex}`\pi`{=tex}.
\]

At (T=84), implement the basis ({1,q_1,b}).

Stable transforms:

\[ H_0(t;L) = L e\^{iz}`\frac{\sin z}{z}`{=tex},
`\quad `{=tex}z=`\frac{Lt}{2}`{=tex}, \]

\[ H_b(t;L) = `\frac{L^3}{2}`{=tex}e\^{iz}
`\frac{\sin z-z\cos z}{z^3}`{=tex}. \]

At (t=0), use analytic limits; never divide interval balls containing
zero by powers of (t).

Identity:

\[ H_b=-`\frac{2i}{t}`{=tex}H\_{q_1}. \]

Stable product formulas:

\[ \|H_0\|^2=L^2A(z)\^2, \]

\[ `\operatorname{Re}`{=tex}(`\overline{H_0}`{=tex}H_b) =
`\frac{L^4}{2}`{=tex}A(z)B(z), \]

\[ \|H_b\|^2=`\frac{L^6}{4}`{=tex}B(z)^2. \]

## Required API

``` python
finite_weil_entry(i, j, L, T, backend)
finite_weil_even_block(L, T, backend)
finite_weil_odd_pivot(L, T, backend)
finite_weil_degree2(L, T, backend)
```

Return structured values, not bare tuples:

``` python
{
  "G00": ...,
  "G0b": ...,
  "Gbb": ...,
  "E2": ...,
  "O1": ...,
  "full_det": ...,
  "normalization": "G = G0 - Gp + Ginf",
  "cutoff_T": 84
}
```

------------------------------------------------------------------------

# 7. WO-RH-13 --- Reproduce Run-16 direct Fourier point certificates

Before uniform interval coverage, reproduce the three independent
anchors.

Required L values:

\[ L=`\log3`{=tex}, \]

\[ L=1.1059499883568553, \]

\[ L=`\log4`{=tex}. \]

Notebook regression values:

### At (L=`\log3`{=tex})

\[ G\_{00,84}`\approx0.107356700414591762`{=tex}, \]

\[ G\_{0b,84}`\approx0.000461820208771810`{=tex}, \]

\[ G\_{bb,84}`\approx3.4253778646359`{=tex}`\times10`{=tex}\^{-5}, \]

\[ E\_{2,84}`\approx3.4640947469748`{=tex}`\times10`{=tex}\^{-6}. \]

### Interior anchor

\[ G\_{00,84}`\approx0.102776248577958063`{=tex}, \]

\[ G\_{0b,84}`\approx0.000783664417448233`{=tex}, \]

\[ G\_{bb,84}`\approx3.9317062336051`{=tex}`\times10`{=tex}\^{-5}, \]

\[ E\_{2,84}`\approx3.4267302528306`{=tex}`\times10`{=tex}\^{-6}. \]

### At (L=`\log4`{=tex})

\[ G\_{00,84}`\approx0.069433265743841579`{=tex}, \]

\[ G\_{0b,84}`\approx-0.000656968872855926`{=tex}, \]

\[ G\_{bb,84}`\approx0.000354436660096118`{=tex}, \]

\[ E\_{2,84}`\approx2.417808670991184`{=tex}`\times10`{=tex}\^{-5}. \]

These values are regression targets, not warrants.

Emit:

``` text
math/rh_weil/certificates/e1_fourier_T84_points.json
```

only if the three point enclosures are rigorous.

------------------------------------------------------------------------

# 8. WO-RH-14 --- Exact support-length jets

This is the key Run-17/18 implementation gap.

For (n`\ge1`{=tex}):

\[ `\partial`{=tex}\_L\^nH_0 = (it)^{n-1}e^{itL}. \]

For the bubble:

\[ `\partial`{=tex}\_LH_b=`\int`{=tex}\_0^Lxe^{itx},dx, \]

\[ `\partial`{=tex}\_L^2H_b=Le^{itL}, \]

and for (n`\ge3`{=tex}),

\[ `\partial`{=tex}\_L\^nH_b = e\^{itL} `\left[
L(it)^{n-2}
+
(n-2)(it)^{n-3}
\right]`{=tex}. \]

For transform pair (U,V),

\[ `\partial`{=tex}*L\^n `\operatorname{Re}`{=tex}(`\overline `{=tex}UV)
= `\sum`{=tex}*{k=0}\^n `\binom `{=tex}nk `\operatorname{Re}`{=tex}
`\left`{=tex}( `\overline{\partial_L^kU}`{=tex}
`\partial`{=tex}\_L\^{n-k}V `\right`{=tex}). \]

For

\[ E=ac-s\^2 \]

with (a=G\_{00}), (s=G\_{0b}), (c=G\_{bb}),

\[ E'=a'c+ac'-2ss', \]

\[ E'' = a''c+2a'c'+ac'' - 2\[(s')\^2+ss''\]. \]

Implement jets analytically. No finite differences are allowed in an E1
path.

Required:

``` text
math/rh_weil/src/fourier_jets.py
math/rh_weil/tests/test_fourier_jets.py
```

Tests must compare analytic jets against high-precision numerical
differentiation at noncritical sample points, but the numerical
differentiation is only a test oracle.

------------------------------------------------------------------------

# 9. WO-RH-15 --- Uniform direct-Fourier T=84 E1 certificate

## Target

Close the latest notebook bottleneck:

\[ E\_{2,84}''(L)\>0 `\quad`{=tex} L`\in[\log3,1.20]`{=tex}, \]

and

\[ E\_{2,84}'(L)\>0 `\quad`{=tex} L`\in[1.20,\log4]`{=tex}. \]

Run-18 numerical guide values:

-   apparent minimum near \[ L\_\*`\approx1.1059498113019184`{=tex}; \]
-   determinant there \[
    E\_{2,84}`\approx3.42673025273`{=tex}`\times10`{=tex}\^{-6}; \]
-   curvature there \[
    E\_{2,84}''`\approx1.43524275454`{=tex}`\times10`{=tex}\^{-3}; \]
-   sampled margin on first region \[
    `\min `{=tex}E''`\approx3.65411`{=tex}`\times10`{=tex}\^{-4}; \]
-   sampled positive derivative margin after the minimum \[
    `\min `{=tex}E'`\approx3.91985`{=tex}`\times10`{=tex}\^{-5}. \]

These guide adaptive subdivision only.

## Certification strategy

Preferred strategy:

1.  interval-cover `[log(3),1.20]` and prove `E2_second.lower > 0`;
2.  interval-cover `[1.20,log(4)]` and prove `E2_first.lower > 0`;
3.  choose a rational center (L_c) near `1.1059498113`;
4.  obtain interval balls for (E(L_c)) and (E'(L_c));
5.  use a certified lower curvature (m\>0) and \[
    `\inf `{=tex}E`\ge `{=tex}E(L_c)-`\frac{E'(L_c)^2}{2m}`{=tex}; \]
6.  require the resulting lower endpoint to be positive.

Do not hard-code the notebook minimum.

## Certificate

``` text
math/rh_weil/certificates/e1_fourier_T84_uniform_degree2.json
```

Required fields include:

-   exact domain;
-   cutoff;
-   normalization;
-   interval backend/version;
-   working precision;
-   cell count and maximum depth;
-   minimum certified `E''` on region A;
-   minimum certified `E'` on region B;
-   point-ball data at (L_c);
-   derived uniform determinant lower bound;
-   source hashes;
-   dependency certificate IDs;
-   `rh_proof_claim: false`.

When this certificate passes, update WO-RH-05 status to
`done_E1_uniform_true_weil_gram`.

------------------------------------------------------------------------

# 10. Optional strengthening: direct scalar and odd shape certificates

Run 18 numerical evidence also suggests:

\[ G\_{00,84}''\>0 \]

throughout the cell, and

\[ O\_{1,84}''\<0 \]

throughout the cell.

These are useful independent checks but are secondary to the even
determinant.

If inexpensive, certify them and emit:

``` text
e1_fourier_T84_scalar_shape.json
e1_fourier_T84_odd_shape.json
```

Do not delay the degree-2 target solely to optimize these bounds.

------------------------------------------------------------------------

# 11. WO-RH-16 --- PIR bridge

Create:

``` text
math/rh_weil/src/pir_bridge.py
math/rh_weil/scripts/export_pir_facts.py
math/rh_weil/certificates/pir_facts.json
math/rh_weil/tests/test_pir_bridge.py
```

Follow the existing B13 bridge pattern.

Mapping:

  -----------------------------------------------------------------------
  Artifact                PIR evidence            Analyzer
  ----------------------- ----------------------- -----------------------
  exact identities        E0                      SOUND

  scalar structural cell  E0                      SOUND

  scalar interval         E1                      SOUND
  positivity                                      

  degree-1 interval       E1                      SOUND
  positivity                                      

  compact degree-2        E1                      SOUND
  positivity                                      

  Fourier heuristic scan  E3                      HEURISTIC + warning

  T=84 point interval     E1                      SOUND, but point-only
  cert                                            content

  T=84 uniform cert       E1                      SOUND

  Connes-CvS external     evidence matching its   
  cross-check             actual warrant; never   
                          promote Atlas theorem   
                          solely from external    
                          agreement               
  -----------------------------------------------------------------------

Recommended coordinates:

``` text
pir_level: L2
layer: UNIVERSAL
namespace: invariant
status: SUPPORTED
```

Every fact content must explicitly include:

``` json
{
  "claim_scope": "finite_dimensional_weil_compression",
  "rh_proof_claim": false
}
```

The E3 scan MUST include a located warning stating that it is an energy
probe and not the true Weil Gram.

------------------------------------------------------------------------

# 12. Connes-CvS cross-validation advancement

The current XC-01/02/03/06 results are useful.

Do not block E1 Atlas certificates on Connes-CvS.

After the true finite Weil Gram is implemented:

## XC-04 upgrade

Replace the current multiplier-only probe with a comparison of a
complete shared scalar quadratic form at common `(L,T)`.

## XC-05 projection bridge

Implement an explicit projection of Atlas test functions into the CvS
trigonometric basis. Compare quadratic forms after projection, not raw
matrix indices.

XC-05 remains diagnostic/external corroboration unless its
truncation/projection error is itself rigorously bounded.

------------------------------------------------------------------------

# 13. CI design

Add two tiers.

## Fast/default CI

-   exact E0 tests;
-   scalar structural tests;
-   Fourier transform/jet identities;
-   PIR schema tests;
-   certificate schema/provenance tests.

## Rigorous RH job

Install:

``` text
python-flint
mpmath
connes-cvs==0.3.1   # optional external-crosscheck job
```

Run:

``` text
python math/rh_weil/scripts/certify_scalar_e1.py
python math/rh_weil/scripts/certify_degree1_e1.py
python math/rh_weil/scripts/certify_degree2_compact_e1.py
python math/rh_weil/scripts/certify_fourier_T84_points.py
python math/rh_weil/scripts/certify_fourier_T84_uniform.py
python math/rh_weil/scripts/export_pir_facts.py
```

CI MUST fail if an existing E1 certificate regenerates to a nonpositive
bound, loses interval coverage, changes normalization, or changes source
hashes without certificate regeneration.

------------------------------------------------------------------------

# 14. Certificate honesty rules

1.  E0 = exact/algebraic only.
2.  E1 = machine-rigorous interval/numerical certificate.
3.  E3 = exploratory floating computation.
4.  Never infer E1 from agreement between two E3 implementations.
5.  Never use imported notebook values as warrant.
6.  Never use `E2_probe` as the Weil determinant.
7.  Every finite-cutoff certificate names `T`.
8.  Every bounded-domain certificate names the exact `L` interval.
9.  Every certificate includes `rh_proof_claim: false`.
10. No wording such as "RH verified", "RH established", or "proof of
    RH".

------------------------------------------------------------------------

# 15. Completion gates

The sprint is complete only when:

``` text
[x] existing E0 tests remain green
[ ] e1_scalar_log3_log4.json regenerated
[ ] e1_degree1_log3_log4.json regenerated
[ ] e1_degree2_compact_log3_log4.json regenerated
[ ] true finite Weil T=84 Gram implemented
[ ] Run-16 three point certificates reproduced rigorously
[ ] exact H0/Hb L-jets implemented
[ ] E2,84'' > 0 certified on [log3,1.20]
[ ] E2,84' > 0 certified on [1.20,log4]
[ ] uniform E2,84 > 0 E1 certificate emitted
[ ] PIR facts emitted and schema-valid
[ ] E3 probe remains clearly quarantined
[ ] Atlas CI green
[ ] no RH proof claim
```

Only after all gates pass may WO-RH-08 / degree 3 be unblocked.

------------------------------------------------------------------------

# 16. Next mathematical stage after parity

Once the repository reaches Run-18 parity, begin the odd degree-3 block:

\[ `\operatorname{span}`{=tex} `\left`{=tex}{ q_1=x-`\frac `{=tex}L2,;
b_3=x(L-x)`\left`{=tex}(x-`\frac `{=tex}L2`\right`{=tex})
`\right`{=tex}}. \]

Known prime kernels to encode as E0 regression identities:

\[ K\_{q_1b_3} = `\frac{(L-a)^2
(L^3+2L^2a-12La^2-6a^3)}{60}`{=tex}, \]

\[ K\_{b_3b_3} = `\frac{(L-a)^3
(L^4+3L^3a-15L^2a^2-18La^3-6a^4)}{420}`{=tex}. \]

The individual active shift block is indefinite on the current cell, so
termwise PSD domination must not be attempted.

Degree 3 is explicitly out of scope until the Run-18 parity gates above
are green.

------------------------------------------------------------------------

# 17. Agent handoff

The implementation agent should work in small commits:

1.  `rh: add rigorous interval backend and scalar E1`
2.  `rh: regenerate degree1 E1`
3.  `rh: regenerate compact degree2 E1`
4.  `rh: implement true finite Weil T84 Gram`
5.  `rh: add analytic support-length jets`
6.  `rh: certify uniform T84 degree2`
7.  `rh: emit PIR facts`
8.  `rh: wire rigorous CI and external crosschecks`

Each commit must leave the default test suite green. Do not overwrite
existing Atlas or Connes-CvS work. Add migrations/deprecations when
changing the E3 Fourier probe API.
